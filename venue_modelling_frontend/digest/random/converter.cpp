static void main(string args[]){
	//while loop checking sql for length
		//if length > 1 execute func() 
}

//func() 
	//gets info from sql like dimensions and audio file location and other setup args
	//digests
	//spits out file in out folder with same name as input folder
	//deletes row
